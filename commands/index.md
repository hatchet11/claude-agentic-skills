---
description: Rebuild the vault master index (master-index.md)
---

Rebuild `~/vault/master-index.md` by scanning all vault subfolders.

Use the Bash tool to run:
```
python3 ~/agentic-os/memory/index_builder.py
```

Print the output, then confirm to the user how many files are now indexed and where the master index lives.
